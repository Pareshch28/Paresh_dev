// Replace with your real GA4 Measurement ID before deploying.
// Get this from Google Analytics > Admin > Data Streams.
export const analytics = {
  measurementId: "G-7176RVEMHD",
  enabled: true, // set to false to fully disable GA4 (e.g. during local dev)
};
